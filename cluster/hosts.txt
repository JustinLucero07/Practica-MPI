# Hostfile del cluster. Una linea por nodo.
# "slots" = numero de procesos MPI que admite ese nodo.
#
#   172.20.10.5 = maquina de justin (maestro/host)
#   172.20.10.8 = maquina de wimer  (trabajador)
#
# Ejecucion desde el maestro:
#   mpiexec -n 4 -hostfile hosts.txt python3 mpi_app.py --estaciones 8 --ciclos 20
#
10.0.2.4 slots=2
10.0.2.3 slots=2
